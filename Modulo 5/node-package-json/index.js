/** como fazemos para acessar os parâmetros passados 
 * na linha de comando para o Node? 
 * R: Através da propriedade process.argv[2] sendo 2 o parâmetro
 * passado como argumento, pois 0 é o próprio node e 1
 * o arquivo a ser executado*/

